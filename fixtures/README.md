# fixtures

Real payloads captured from live `claude` sessions.
They are recordings, not hand-written mocks: the replay tests in
`mac/ccd/src/fixture_replay.rs` run them through the production ingest path, so
a schema drift in a future Claude Code release fails the test suite instead of
failing silently in production.

| Path | What it is |
|---|---|
| `hooks/acceptance-run.jsonl` | Every hook payload from one end-to-end `codeconnect claude` session that went through `cc-hook` -> `ccd` (SessionStart, PreToolUse, PermissionRequest, Notification, PostToolUse, Stop). Daemon-added fields stripped. |
| `hooks/live-hook-payloads.jsonl` | Deduplicated payloads from the hook-semantics probes, including the `permission_prompt` Notification and multiple PermissionRequest shapes. |
| `appattest/attestation-object.b64` | Apple's own published App Attest attestation object, base64 at 76 columns, 5906 bytes decoded. From the DocC payload behind <https://developer.apple.com/documentation/devicecheck/attestation-object-validation-guide> — the rendered page is a script shell, the JSON carries the object. It is the only genuine Apple chain in existence to test against, and the only thing that catches Apple's P-384-key/SHA-256-signature pairing: a chain minted at test time cannot reproduce it, so a narrowed algorithm list would pass every synthetic test and reject every real device. Its credential certificate was valid for three days in April 2026 and has expired, which is why `mac/push-relay/src/attest.rs` takes the verification time as a parameter and its tests pin a clock inside that window rather than reading the system one. |
| `transcript/session-sample.jsonl` | A real session JSONL slice covering `mode`, `permission-mode`, `file-history-snapshot`, `user`, `attachment`, `ai-title`, `assistant` and `system` entries. |
| `panes/composer/` (10) | Screens with a composer that is taking keys: every permission mode, mid-turn, a typed prompt, prompt text wrapped over three rows, a transcript full of `❯` echoes, and a pane three columns wide. The presence check must accept every one. |
| `panes/no-composer/` (6) | Screens with no composer to type into: the permission prompt, the model menu, the rewind menu, the trust dialog, `/status`, and a plain shell that printed a `❯`. Every one of them carries a `❯` somewhere, which is what makes them the set worth having. The presence check must refuse every one. |
| `panes/bounds/` (2) | Real composers the presence check refuses, and must go on refusing. `tall-composer-top-rule-offscreen.txt` holds more text than its 12-row pane is tall, so its opening rule has scrolled off and the `❯` row is the pane's first row. `short-pane-top-rule-clipped.txt` is a four-row pane where the opening rule is clipped and only the *closing* rule is left, directly below the marker. See the tests that name them. |
| `codex/composite_ids.json` | Rust-generated composite wire-id vectors (`protocol::composite_id`); the Swift client's opaque-correlation cross-language pin. |
| `codex/lifecycle.jsonl` (14) | One live-observed Codex app-server turn — the real 0.147 notification stream (`thread/started`, `item/started`/`completed` for a userMessage and an agentMessage, the streamed `item/agentMessage/delta`, `thread/tokenUsage/updated`, `turn/completed{completed}`), plus the observation-noise frames the adapter deliberately drops. Each line is one JSON-RPC frame, exactly as a connection delivers it. Captured from `captures/subscribed/`; from a throwaway project, absolute user paths scrubbed to `/work/…`. Drives `codex_adapter.rs`. |
| `codex/command-execution.jsonl` (30) | A turn that runs a shell command — `commandExecution` `item/started`(inProgress)→`item/completed`(exitCode 0), with `reasoning` items and the `item/commandExecution/requestApproval` + `serverRequest/resolved` frames (approvals are Phase 3; the observation adapter drops them). From `captures/approvals-accept/`. |
| `codex/file-change.jsonl` (44) | A `fileChange` item (`changes[].{path,kind,diff}`) started, approval-requested, and completed. From `captures/approvals-fc-readonly/`. |
| `codex/thread-switch.jsonl` (169) | **The `/new` thread switch, measured on four connections at once** (chunk 2e-4c). One live 0.147 session driven through: a turn on thread A, an observer resumed to A, `/new`, a turn on B, that same observer resumed to B, a `/resume` back to A, and an `thread/unsubscribe`. Each line carries `conn` (which connection delivered it) and `dir` (`c2s`/`s2c`), plus `note` markers naming each step, because the whole point of the capture is *who got what*: the switch signal, the frames a subscription does and does not receive, and the ordering of the `thread/unsubscribe` markers. Observation noise (`plugin/list`, `app/list`, skills, account) and the multi-MB frames are excluded; absolute paths scrubbed to `/work/…`. |
| `codex/session-0.153.jsonl` (16) | **The codex 0.153 re-grounding capture** — one live `codeconnect codex` session on 0.153, recorded by the in-broker frame tee (`codex_broker::frame_tee`, a `--features frame-tee` build plus `CC_CODEX_FRAME_TEE=<path>`), which is the first committed capture taken by tooling that lives in this repository. Sanitised to the `{conn,dir,frame}` shape the older captures use — the tee also records each frame VERBATIM as `raw`, plus `run`/`seq`, which a fixture does not need and which cannot be scrubbed the way a parsed view can. Carries the three frames the 0.153 pins are grounded on: the `thread/start` whose `dynamicTools` declares the `codex_tui` bundle, the `turn/start` carrying `serviceTierForTurn`/`toolOutput`/`turnTrigger`/`cyberAccessProgram` all present-and-null, and the `thread/read` on a FOREIGN thread that `Disposition::ReadSessionThread` refuses. `item/tool/call` shows the model invoking `read_thread` and `wait_threads` with a foreign `threadId`. Streaming deltas, token usage and bootstrap reads excluded as observation noise; absolute paths scrubbed to `/work/…`, the account name length-preserved. **Broker refusals are not in it**: a refusal is synthesised locally and never crosses the upstream leg the tee taps, so this file carries the refused REQUESTS and the verdicts live in the run dir's `broker.log`. |
| `codex/thread-start-operator-config-0.153.jsonl` (1) | **The first capture taken against a real operator's `config.toml`**, rather than the empty test `CODEX_HOME` (`auth.json` and nothing else) that every live gate and both captures above build. One `thread/start` from a live `codeconnect codex` on 0.153, same frame tee and same sanitised `{conn,dir,frame}` shape as `session-0.153.jsonl`; the one absolute path (`runtimeWorkspaceRoots`) scrubbed to `/work/…`. It exists because that difference is load-bearing: on a **creation frame** the TUI sends `serviceTier` only when the user's own config sets `service_tier` (measured again on 0.153.4, whose `thread/start` is 23 keys without it), so no capture taken against an empty config could ever have carried it, and `thread/start`'s exhaustive allowlist refused the key as unknown — which killed the session outright, because codex 0.153's TUI exits the moment its first `thread/start` is refused. This file is the measurement that widened the allowlist by one preference field (`codex-broker/src/fingerprint.rs`, `check_thread_start_service_tier`), and the union invariant reads it directly, so the rule and its evidence cannot drift apart. Its 24 keys are the 23 of the 0.153 capture plus `serviceTier`; `sandbox` reads `read-only` because CodeConnect now passes the session sandbox to the TUI explicitly. **The "only when the config asks" qualifier is per-METHOD and it was learned the hard way** — on 0.153.4 the same TUI puts `serviceTier` on every `turn/start` with no config at all; see `codex/turn-start-servicetier-0.153.4.json`. |
| `codex/turn-start-servicetier-0.153.4.json` (1) | **The 24-key `turn/start` that refused every hosted turn on codex 0.153.4.** One `turn/start` off the TUI leg, frame tee, same sanitised `{conn,dir,frame}` shape as the captures above; absolute paths scrubbed to `/work/proj`. The 24th key is `serviceTier: "default"` — and unlike the creation frame above, it is **not** config-induced: measured twice, once against a `CODEX_HOME` with no `config.toml` at all on the account's default model, and once against a scratch config pinning only `model`, both carrying the key. `TURN_START_CAPTURED_PARAMS` pinned 23, so `turn/start params: unknown top-level parameter (1 of 24)` refused every turn and the pane read `Failed to start turn … request refused by session policy (code -32001)`. This file is the measurement that widened the turn allowlist by one preference field (`codex-broker/src/fingerprint.rs`, `check_turn_start_service_tier`), and the union invariant `the_captured_param_set_is_exactly_the_grounded_captures` reads it directly, so the rule and its evidence cannot drift apart. |
| `codex/steer-0.153.4.jsonl` (16) | **What the TUI actually sends when you type into a running turn on 0.153.4: `turn/steer`, immediately.** One live session, frame tee, `{conn,dir,frame}`; paths scrubbed to `/work/proj` (`runtimeWorkspaceRoots` normalised to agree with `cwd`, which they do on the wire). Not a queued `turn/start` and not a second turn: the reply names the RUNNING turn's id, no `turn/started` is emitted, and the only terminal is the original turn's `turn/completed`. Its six params carry no `cwd`/`sandboxPolicy`/`approvalPolicy`, so a steer structurally cannot move the launch fingerprint — but it does carry `expectedTurnId`, a staleness guard a `turn/start` has no equivalent of. Evidence for the 4b steer decision; nothing is grounded on it yet. |
| `codex/steer-refusals-0.153.4.txt` | **Who may steer or start a turn on 0.153.4, per leg, one request per row** — the app-server's own socket behind a byte-transparent logging proxy, beside the broker's ccd and TUI legs, plus the broker's own `broker.log` verdicts. Two measurements worth naming: the app-server has **no per-connection ownership** (a second connection starts turns on the head thread freely — everything protecting that is the broker), and a plain `turn/start` sent while a turn is BUSY returns the RUNNING turn's id, i.e. the server treats it as a steer with none of `turn/steer`'s guards. Also the pre-fix `serviceTier` refusal, verbatim, as it reached `broker.log`. |
| `codex/compose-refusals-0.153.4.txt` | **What a `turn/start` needs from the CLIENT that sends it, and the one param that is gated on it.** The app-server's own socket, no broker. codex 0.153.4 refuses `turn/start.runtimeWorkspaceRoots` with `-32600 "requires experimentalApi capability"` for any connection that did not declare `capabilities.experimentalApi` at `initialize` — the real TUI declares it, the CodeConnect control link declares `clientInfo` and nothing else — while the same frame WITHOUT that key, `cwd` still present, is accepted. The `initialize` RESULT is byte-identical either way, so the capability advertises nothing and gates params at use time. **It was found by a live gate and could not have been found by a unit test**: the broker admits the frame and the refusal comes from the server behind it, so the first run of the 4b compose gate is in the file verbatim, refusal and all. It is the measurement behind two decisions — the phone's `turn/start` omits `runtimeWorkspaceRoots`, and the ccd leg does NOT declare the capability, because declaring it would buy one field by handing the phone's leg the experimental parameter surface of the whole API. Cited from `codex-broker/src/session.rs` (the `roots_may_defer` rule) and `ccd/src/codex_link.rs` (`compose_frame`). |
| `codex/compose-0.153.4.jsonl` | **A phone starting a real turn and then steering it, end to end.** Written by the 4b live gate itself (`ccd/src/codex_link_live.rs`, `CC_CODEX_COMPOSE_CAPTURE=<path>`), so the rule and its evidence cannot drift apart. Four `phone` rows — the two `compose` asks and the two `compose_result`s they were answered with — beside the frames the subscribed ccd observer saw for that turn. The point is what is NOT there: the steer produces **no second `turn/started`**, its `compose_result` names the SAME turn id the start was given, and one `turn/completed` ends both. That is the whole difference between a steer and a start, on the wire, from the leg a phone reaches. Absolute paths scrubbed to `/work/…`; the prompts are placeholders because their content is this gate's business and not the wire's. Two `ccd` rows carry the other half, and their provenance is exactly this: the fourteen-key `turn/start` **received by the broker on the ccd leg** — `compose_frame`'s own output, not a copy of it — and the `-32001 turn refused: this session is already running a turn` the broker answered it with. **Params only, and zero upstream bytes:** the thread was busy, so the app-server never saw this frame, and the rows carry no JSON-RPC ids because they are reconstructed from `compose_frame()["params"]` and the refusal rather than teed off the socket. That is enough for what they are for — `codex_broker::refusal` pins its `PHONE_TURN_PARAMS` to that row's key set, so the list the broker refuses everything outside of is held to a frame a real producer emitted and a real broker read, rather than to a second hand-written copy of itself. |
| `codex/approval-0.153.jsonl` (29) | **The approval wire on codex 0.153, captured on the ccd leg itself.** Two live `codeconnect codex` sessions — one driven to a `commandExecution` approval, one to a `fileChange` — recorded by the measurement probes in `ccd/src/codex_link_live.rs`, in the `{conn,dir,frame}` shape the older captures use. It exists because 0.147's captures cannot show three things this one does. **`availableDecisions` is on the STABLE wire**, populated with exactly three of the schema's six (`accept`, `acceptWithExecpolicyAmendment`, `cancel`) — the generator declares that field only in its *experimental* bundle, and the launch carries no `--experimental`, so the bundle's label describes the documentation and not the frame. **`reason` is populated** (0.147 sent it null), which is what a human reads first. And the `fileChange` request carries **no content and no options at all** — `reason: null`, `grantRoot: null`, nothing else — while its `changes[].{path,kind,diff}` ride the `item/started` immediately before it, which is why a file-change card is a join and why the observer refuses to card one it cannot find. The `conn` names which leg was tapped; `ccd-command-unsubscribed` is the control connection, which resumed nothing and received **zero** approvals. Observation noise (token usage, rate limits, status/goal changes, streamed deltas, `turn/diff/updated`, `deprecationNotice`) excluded; absolute paths scrubbed to `/work/…`. `ccd/src/codex_approval.rs` replays this file through the production parser, so a schema drift in a future codex release fails the suite. |
| `codex/approval-rebind-0.153.jsonl` (26) | **A daemon bounce, mid-approval, on both legs.** One live `codeconnect codex` 0.153.2 session driven to a `commandExecution` approval and then *left there* while the subscribed ccd leg is closed outright and a replacement one resumes — the transition the whole rebind design rests on and the one no earlier capture drove. It answers the question three ways were possible for: the app-server **re-delivers** the outstanding `item/commandExecution/requestApproval` to the replacement connection, with a **byte-identical `itemId`** and a wire `id` of `0` *again* — the same integer the first leg was handed, which is exactly what the wire id is worth across a reconnect: nothing. It also settles what the resume answer is **not**: the `thread/resume` request and its response are both here (`dir` `c2s` then `s2c`), and that response's `turns[]` — `["completed", "inProgress"]` — never names the pending item, so a resume answer is not a witness for an open approval and the re-delivery is the only one a rebound link gets. `codex_link.rs`'s bounce test reads that response out of this file and asserts it, rather than trusting a constant in a source file it never opens. The last frames are the terminal: the operator answered at the keyboard and the `serverRequest/resolved` reached the *replacement* leg, which is what keeps a bounced daemon from leaving an answered question on a phone. `conn` names which leg was tapped (`ccd-before-bounce` / `ccd-after-bounce`) and `dir` which way the frame went. **What is here and what is not:** every frame either leg received, responses included, plus the one request the replacement leg sent — nothing received is filtered out, because on this capture the ordinary frames are the evidence about what a rebound leg is and is not handed. The handshake the harness sends before resuming (`initialize`, `initialized`) is not recorded; it is harness bookkeeping, not app-server behaviour. Absolute paths scrubbed to `/work/…` and the sandbox rollout path to a fixed name. Written by the live gate itself (`ccd/src/codex_link_live.rs`, `CC_CODEX_BOUNCE_CAPTURE=<path>`) — so the rebind rule and the frames it was derived from cannot drift apart. |
| `codex/approval-switch-p1-ctrlc-new-0.153.jsonl` (6) · `codex/approval-switch-p4-esc-new-0.153.jsonl` (6) | **A thread switch does not cross an unresolved approval on 0.153.2 — the raw s2c order, one file per probe.** Each is one live single-session capture on the subscribed ccd leg, in arrival order, of a command approval interrupted from the keyboard (P1: Ctrl-C then `/new`; P4: Esc then `/new`). The order is the point: `serverRequest/resolved` lands FIRST, then `item/completed{status:"declined"}`, then `turn/completed{status:"interrupted"}`, and the new thread's `thread/started` only ~1.15–1.25 s LATER (the app-server's own `emittedAtMs` are on the frames). So the switch is admitted only after the approval has fully resolved and its capability has been consumed; the daemon's `codeconnect/responseDisposition{delivered:false,winner:"tui"}` on the late re-answer is the wire confirming the capability was already the keyboard's. **This is a DIFFERENT scenario from A25's `interrupt.jsonl`, not a contradiction of it:** A25 measured Ctrl-C on a running turn with NO approval prompt up, where the in-flight command never gets an `item/completed`; here the prompt is showing, so the interrupt is consumed as the prompt's own decline and the item IS declined — which is why `resolved` leads the turn terminal. Both orderings are real for their own scenario. Only the decisive s2c frames are kept (token usage, rate limits, status/goal changes, streamed deltas excluded, as the other 0.153 captures do); absolute paths scrubbed to `/work/…`. |
| `codex/approval-refusals-0.153.jsonl` (9) | **The three `refuse_message()` refusals a switch producer meets while an approval is pending, one per FRAME ID.** The ccd leg's `thread/start` (id 8100 → `not permitted for this endpoint role`) and the `thread/unsubscribe` that would reserve a switch behind it (id 8101 → `not permitted for this connection`); and a **fully fingerprinted** second TUI-leg `thread/start` — the one an earlier probe got wrong by sending `{"cwd":"/tmp"}` and being refused by fingerprint before any switch decision — which reaches the session-policy decision and is refused there (id 101 → `request refused by session policy`). `refusal.rs`'s `the_captured_switch_refusals…` pins each by its id against `refuse_message` today; **if a refusal is reworded, update the const in `refusal.rs`, never edit this .jsonl.** |
| `codex/approval-switch-p2p3-0.153.jsonl` (7) | **The other two switch producers, refused before they could switch, with an approval pending — evidence, not `refuse_message()` pins.** P2: a fingerprinted second-TUI `thread/start` naming a non-launch workspace is refused at the fingerprint boundary (`names a workspace other than the session's launch workspace`). P3: the ccd leg's one permitted method, `thread/resume`, aimed at a thread OTHER than the card's, is refused `target thread is not bound to this session` (a resume to the card's own thread is instead accepted as a rebind and leaves the head where it is). **What the broker enforces:** a completed switch requires a head-moving `thread/start`, and that is what the live-turn/approval gate fences — a creation is refused while a turn or an approval is live. A `thread/resume` does not move the head at all: it subscribes the connection to the thread it names and returns that thread's own state, so even a resume to a session-bound thread cannot switch the head. That covers the resume the ccd leg is permitted, including the one case the refusals above do not — a resume to a DIFFERENT but session-bound (retired) thread, which is measured accepted-and-head-unmoved in `approval-switch-p5-ccd-resume-bound-0.153.jsonl`. This file is broker-gate + wire-refusal evidence, not a completed-switch capture. **Residual:** a real TUI `/resume` or `/fork` is not captured end-to-end, because the parked test coordinator (`--test-bringup hang`) cannot service the re-exec a real `/resume` triggers, so the TUI exits. |
| `codex/approval-switch-p5-ccd-resume-bound-0.153.jsonl` (7) | **The one resume the refusals above do not cover: the ccd leg resumes a DIFFERENT session-bound thread while an approval is pending, and the head does not move.** A session driven to two bound threads — a first thread run and then `/new`'d into a retired-but-bound thread T1, a fresh head T2 — with the command approval raised on T2. The ccd leg then sends `thread/resume` for T1, which IS session-bound (unlike P3's foreign id, which the binding check refuses), so it is ACCEPTED — and the response carries T1 with `status` `idle`: the head stays T2, no new thread starts, and T2's approval `serverRequest/resolved` is reached by the ccd leg's own late answer, so the capability never left it. The measurement that closes "a bound resume could switch the head": it cannot, because a resume subscribes and only a head-moving `thread/start` moves the head. Raw s2c on the subscribed ccd leg, absolute paths scrubbed to `/work/…`; asserted by `p5_ccd_resume_to_a_second_bound_thread_with_an_approval_pending` in `ccd/src/codex_link_live.rs`. |
| `codex/approval-switch-panes-0.153.txt` | **The three screens behind that file.** (1) the command-approval prompt as 0.153.2 renders it — `Would you like to run the following command?`, the three options with their hotkeys (`y` / `p` / `esc`) and `Press enter to confirm or esc to cancel`, which is what makes `Enter` the prompt's accept and not the composer's submit; (2) the same pane after `/new` was typed into it with **nothing submitted** — the request cancelled, the turn interrupted, and only the residual `ew` reaching the composer once the question was already terminal; (3) `/new` typed AND submitted while a turn was running with no approval anywhere, answered `■ '/new' is disabled while a task is in progress.` with **no frame reaching the wire** — the same client-side guard measured on 0.147, re-measured here, and the one that closes the question on its own, because an approval exists only inside a turn. `tmux capture-pane -p -J`; the probe file's nonce is zeroed length-preservingly, so the pane geometry is byte-identical to what was captured. |
| `codex/approval-amendment-labels-0.153.txt` | **What the TUI calls each decision, and what the wire said the decision was.** Seven live prompts captured with `tmux capture-pane -p -J` in a 200-column window so no option row is clipped — six command approvals and one file change — each shown beside its own request's wire fields, including the `availableDecisions` entry that carries the amendment body production actually reads. It exists because one of the words is a function of the offer, in two ways one measurement cannot separate. **Which part of the request it names:** the TUI renders `acceptWithExecpolicyAmendment` as "Yes, and don't ask again for commands that start with `<argv>`", and the one-token `["touch"]` in `approval-0.153.jsonl` is equally consistent with "the first token", "the whole argv" and "the parsed command" — its own painted row, in `codex/approval-switch-panes-0.153.txt` §1, reads `touch` for a command of `touch /tmp/cc-3c-quiesce.….txt` and so refutes the third on its own; a two-token amendment, a three-token one, and one whose first token is an absolute path separate the other two, and the answer is the amendment's own `execpolicy_amendment` tokens — not the request's `command` (the `/bin/zsh -lc '…'` wrapper the amendment is precisely not about), not `commandActions[0]`, and not a basename. **How those tokens are spelled:** every token in those three is one a shell leaves alone, so a plain join and an escape-then-join render identically and cannot be told apart — so three more prompts drive a token with a SPACE, a token with a METACHARACTER, and an argv that is a `<shell> -lc <script>` wrapper. The first two come back **single-quoted**; the wrapper comes back **unwrapped**, its script printed verbatim. So the rule is: unwrap a login-shell wrapper to its script, otherwise spell each token the way a shell would have to be given it and join with single spaces. An earlier run of the same prompts, same binary, returned ONE-token amendments for five of the six and the TUI painted exactly those — so the same command is offered a narrow amendment on one turn and the whole argv on the next, which is the strongest reason the label is read off the amendment body and never re-derived from `command`. A token carrying a real CR/LF was asked for and not produced, and one needing a quote spliced into its own quoting was not either — `ccd/src/codex_approval.rs` renders neither, falling back to the generic words for an unmeasured spelling and withdrawing the decision entirely for a line break. It derives the label that way and asserts it against this file, so the rule and its evidence cannot drift apart. The file-change family declares no decisions at all and its three words carry nothing command-specific; they are unchanged, and the last section is what that looks like. The probe file's nanosecond stamp is zeroed length-preservingly and each row's trailing padding is trimmed. |
| `codex/nonphone-families-0.153.txt` | **The server-request families a phone does not answer, and what a real session does with them.** The bundle declares three besides the two a phone answers — `item/permissions/requestApproval`, `item/tool/requestUserInput`, `mcpServer/elicitation/request` — and this is the evidence for a claim about a **launch** rather than about codex. The permissions family HAS a producer: a model-callable tool behind `features.request_permissions_tool`, measured `false` by default, `true` when an operator `config.toml` sets it, and `false` again under a `-c features.request_permissions_tool=false`. A shipping launch hands both codex processes the operator's own `CODEX_HOME`, so `codeconnect` writes that override onto each spawn and its reserved argv grammar refuses a caller's `-c` for the same key; the app-server argv in this file is read back out of the process table DURING the run, so the pin recorded is the running process's. With it pinned, five positions were driven — a question put to the user, a network escalation, a filesystem escalation with the command approved, the same with it declined, and an MCP form — and none of the three was produced: `watched_family_delivered=false` is the tap on the subscribed ccd leg and `broker_refusals_naming_one=0` is the broker's own capability log, which is the only witness for a frame the app-server emitted and the broker answered upstream. An MCP server was connected and a tool on it was called, so the elicitation zero is a driven zero rather than an absent server. The live probe ASSERTS these zeros rather than printing them. The two undelivered families do not end in `/requestApproval`, so the broker never hands either to any leg — which is also why the daemon has no opinion about them, and why their zero here is a fact about this model's behaviour rather than a wire property. |
| `codex/approval-card-0.153.json` | **The approval card contract with the iOS app**, one member per request family. Not a wire capture: it is what `ccd::codex_approval` *produces* from the real 0.153 `item/commandExecution/requestApproval` and `item/fileChange/requestApproval` frames, so it is the ground truth Phase 5 builds the phone against while this repository still cannot compile the app. Each member decodes as `ios/CodeConnect/Protocol/WireTypes.swift`'s `ApprovalCard`, and the test that reads this file asserts the app's own two gates against it — the five keys the app decodes non-optionally, and `SHA-256(display_text) == payload_hash`, which is what `CardVerification.swift` recomputes before `DecisionCard.swift` will draw a button. It then re-derives the whole file from the frames and requires a byte-identical match, so the fixture cannot go stale without the diff saying so. The `request_id` is a real `protocol::composite_id` value over `(session_uid, thread_id, itemId, generation)` — **the item id, not the wire request id**, which is a per-connection integer from zero and identifies nothing across the reconnect a rebind is made of. The command card's amendment option carries the command-specific words the TUI paints, derived from the amendment argv per `codex/approval-amendment-labels-0.153.txt`; the file-change card's three are unchanged. |
| `codex/minor-19-wire.json` | **The three fields protocol minor 19 adds, as one real session produces them.** Not a wire capture and not hand-written: it is what `ccd` *emits* for one drive of the production path — `Daemon::raise_codex_approval` files the `approval_request`, `Daemon::retire_codex_approval` files the `approval_resolved`, and `Daemon::sessions` assembles the fleet — so the phone, which this repository cannot compile, has the exact bytes to decode against. The `approval_request` carries **`turn_id` on the envelope** (the turn a Stop pressed from that card names; `interrupt` requires one and the card gave the phone none, so the only alternative was inferring it from an earlier event's envelope, which a link that rebound mid-turn never sent). The `approval_resolved` payload carries **`request_id`** beside `status`, where Claude's `AnswerOutcome` has carried one since minor 0 — before it, the only correlation back to the card was stripping `"resolved:"` off `source_event_id`, a prefix parse whose failure is silent and whose symptom is an answered card standing on a phone for ever; the payload still decodes as a bare `CodexResolution`, which is what makes the addition additive. The fleet is two rows: a Codex run whose **`codex_link` is `subscribed`** (and which is `blocked_on` the very card above), and a Claude run whose `codex_link` is `none` — the field that makes per-session Stop/Compose actuatability computable from the fleet, which `ws::Capabilities::codex_interrupt` named as a limit and no longer does. The approval is a real 0.153 `commandExecution` request read by the production parser, and its `request_id` is the **derived** composite over `(session_uid, thread_id, item_id, generation)` — the same codec `codex/approval-card-0.153.json` uses, over this capture's own item rather than that one's, so the two files carry different ids by construction and neither is a hand-written string. **Only the clock is normalised**: the events' `ts` and the summaries' `created_at`/`updated_at` are replaced with a fixed stamp, and the generator asserts each was an RFC3339 read before replacing it. `ccd/src/state.rs` re-derives the whole file and requires a byte-identical match, so it cannot go stale without the diff saying so. |
| `codex/refusal-sentences.json` | **Every sentence `ccd` can send a phone when a Codex interrupt or compose did not happen — 68 of them, each tagged with what the phone may conclude from it.** Emitted, not captured, and for a reason the wire forces: `InterruptResult::Rejected` and `ComposeResult::Rejected` carry a bare `reason` string and **no code**, so the iOS side decides whether to grey Stop and Compose for a few seconds by `contains`-matching the clauses that describe a link which is coming back (`try again shortly`, `try again in a few seconds`, `is not yet watching its thread`). That match is correct for every sentence this build can say and would go silently wrong the first time somebody reworded one: nothing on the Mac side would fail, because the daemon would still be saying something true, and the phone would simply stop believing it. So the sentences were hoisted out of the three files that assembled them inline (`ccd/src/state.rs`'s pre-flight, `ccd/src/codex_link.rs`'s channel, pre-write, claim, wire and settlement paths, and `ccd/src/store.rs`'s ledger replays) into `ccd/src/codex_refusals.rs`, which is now **the one place each is written** — the hoist collapsed five pairs of byte-identical duplicates, including the "already sent and what became of it is not known" sentence that stood in three places at once. Each row carries `verb` (`interrupt` / `compose` / `both`), `outcome` (the wire variant the reason rides in: `rejected` or `indeterminate`) and `category`: **`link_state`** (18 — a fact about the control link *right now*; the same ask a moment later may well be written, and these are the ones worth waiting out), **`permanent`** (43 — the ask was wrong, the run is not a Codex run, the request id is spent, or the outcome is already recorded), **`transient_local`** (5 — this Mac's own store or lookup failed *before* anything was written, so nothing reached Codex and the same ask may well succeed on a retry; distinct from `link_state` because what is unwell is this side's disk rather than the connection, and from `permanent` because nothing about the ask was wrong), **`wire_code`** (2 — the app-server or the broker refused the write, and the sentence passes on their numeric **code** and never their message, which has been measured naming a turn id the phone never sent). The interpolated sentences appear as templates — `{uid}`, `{ref}`, `{agent}`, `{n}`, `{err}`, `{status}`, `{word}`, each explained in the file's own `tokens` map — and they are **assembled by calling the very formatters production calls**, with those tokens as the arguments, so a template here has no second copy to drift from. It is **written by the generator itself** (`cargo test -p ccd --bin ccd -- --ignored regenerate_the_refusal_sentences_fixture`, or `CC_REFUSAL_FIXTURE_OUT=<path>` to write elsewhere) rather than printed for a shell to redirect: libtest owns the same stdout, so a `--nocapture > file` regeneration wrote the harness's own "running 1 test" lines into the JSON. Two sentences the code can still say are deliberately **not** in it — the `INTERRUPT_ABORTED` ledger replay, which `replayed_interrupt_report` answers as a structured `Duplicate` before the sentence path, and the link's own empty-turn guard, which `Daemon::interrupt` refuses several steps earlier — because the file describes what a phone can be told, not what the code can defend against; both consts carry a comment saying so. `ccd/src/state.rs` re-derives the whole file and requires a byte-identical match (`the_refusal_sentences_fixture_is_what_this_build_emits`), and additionally names the five link-state clauses on the committed bytes, so a sentence leaving that family fails the gate saying which one. It carries no path, no identifier and no capture: every byte of it is prose this repository authored. |
| `codex/dynamic-tools-0.153.json` | The `codex_tui` `dynamicTools` bundle verbatim, from that capture — six tools (`list_threads`, `list_archived_threads`, `read_thread`, `wait_threads`, `set_thread_title`, `set_thread_archived`), canonical form 2728 bytes, sha256 `7c962509…`. `codex-broker/src/fingerprint.rs` compares against this file, so the pin and its evidence cannot drift apart. |
| `codex/developer-instructions-0.153.txt` | codex 0.153's `collaborationMode.settings.developer_instructions`, 1288 bytes, sha256 `1042cc64…`. The 0.147 blob is 925 bytes and the **schema for this field is byte-identical between the two releases** — this file is the evidence for the content-drift half of re-grounding, which the launch-time shape gate structurally cannot see. |
| `codex/model-switch.json` | **Eleven real `turn/start` frames across three models** (chunk 2e-4c), plus the six `thread/settings/update` frames the TUI's `/model` flow emits. The evidence behind the `collaborationMode` re-grounding: `mode` and `settings.developer_instructions` byte-identical on all eleven (and identical to `turn-start-request.json`, captured a week earlier on a different sandbox), while `settings.model` and `settings.reasoning_effort` simply carry whatever the picker last set. `codex-broker/src/fingerprint.rs` reads this file directly in its tests, so the rule and its evidence cannot drift apart. |
| `codex/interrupt.jsonl` (36) | Two turns; the second interrupts a `commandExecution` mid-flight — `turn/completed{status:"interrupted", items:[]}` (D14) leaves the exec non-terminal, so the adapter must synthesize its terminal from what it saw live. From `captures/steer-abort-pending/`. |
| `codex/interrupt-0.153.jsonl` | **A whole driven turn lifecycle on 0.153.4, filtered to the turn-status vocabulary, with both interrupt scenarios in it.** Two turns on one live `codeconnect codex` session, each raised to a command approval and then aborted by a `turn/interrupt` sent on the wire — the first with the approval already answered at the keyboard, the second with the prompt still showing. It is the capture a daemon that has to say *running / idle / waiting on a person* reads, and the sequence is the evidence rather than the endings: `thread/status/changed{active,activeFlags:[]}` and `turn/started` arrive in the same millisecond, `activeFlags:["waitingOnApproval"]` is the only frame that says a person is being waited on, `activeFlags:[]` returns in the same millisecond as `serverRequest/resolved`, and `{type:"idle"}` arrives in the same millisecond as `turn/completed` — read off the app-server's own `emittedAtMs`, which every notification carries, the two `requestApproval` requests carrying `startedAtMs` that agrees with the status frame beside them. **The status frame leads its partner everywhere but one place, and that place is the resolution**: `serverRequest/resolved` lands FIRST and the `activeFlags:[]` that clears the wait follows it inside the same millisecond, so a reader holding "status first" as a rule reads exactly that pair backwards. The two interrupt terminals differ in exactly one frame: with no prompt showing the turn simply ends `interrupted` with `items:[]`; with one showing, `serverRequest/resolved` follows the turn terminal in the SAME millisecond, so first-terminal-wins retires the card as a turn abort and the resolution that follows finds it already gone. **That ordering is a fourth scenario, not a contradiction of the other three** — a keyboard Ctrl-C on a running turn with no prompt, a phone `cancel`, and a keyboard Ctrl-C with a prompt showing each have their own, and all four are real for their own producer. The last frame is a `thread/started` for a THIRD thread the capture never runs a turn on — the fresh head the session was left with, carried rather than consumed, and the only frame in the file naming that id. **Its frame count is deliberately not stated here**: the probe that writes the file does not emit the same number of frames on every run, so a count in prose is a pin that goes stale on the next capture rather than a fact about the wire. Observation noise (token usage, rate limits, streamed deltas) excluded as the other 0.153 captures exclude it; every absolute path this machine contributed scrubbed to `/work/…` — the workspace `cwd`, the sandbox rollout path, and the probe's marker file to `/work/marker.txt`, the fixed name the other captures use for the same thing. The two left standing are not this machine's: the `/bin/zsh` of the login-shell wrapper, and the `/tmp` inside the model's own `reason` sentence. Both of its approvals are replayed by the risk class pin in `ccd/src/codex_approval.rs`, which the fixture-directory census requires. Written by the live probe in `ccd/src/codex_link_live.rs` (`CC_CODEX_4A_EVID=<dir>`). |
| `codex/interrupt-refusals-0.153.txt` | **The PRE-ADMISSION baseline for who may interrupt a turn, and what each party said when the answer was no — a historical measurement, and the one that caused the rule it records to be removed.** One request per row, driven live on three connections at once, because the question had three subjects. At capture time the **ccd** leg — the path a phone's interrupt would take — was refused identically in all six shapes (`-32001 method refused: not permitted for this connection`) with zero bytes upstream, *before* any binding was examined, because the ccd allowlist then carried no arm for `turn/interrupt` at all. **Those rows are the evidence that removed that rule, not a description of the leg**: ccd now carries the same gated arm the tui leg has (`Disposition::InterruptActiveTurn` in `codex-broker/src/allowlist.rs`, bound to this session's running turn), so a re-run of the probe does not reproduce them — the file's own header says so before its first row, so neither file can be read as current behaviour. The **tui** leg, where the interrupt rule was already wired, forwards it for a genuinely running turn (`result:{}`) and refuses every other shape with ONE message on the wire — a turn that already ended, a turn id that names nothing and a turn on a retired thread all read "it does not name a running turn of this session"; which binding actually failed is named only in the broker's own log, and that is where an id the session never used reads "was not observed as a session thread". The **app-server's own socket**, with no broker in between, refuses a bad thread (`thread not found`) and a bad turn (`expected active turn id X but found Y`, which hands the caller the real one) — and answers an interrupt for a turn that has ALREADY ENDED with nothing at all, indefinitely. That silence is the reason a caller may never treat a response as its outcome and must bound its own wait; the running-turn gate is what keeps it unreachable. The last section is what the daemon knows when the control link's wire is taken away under a session whose TUI keeps running: `Subscribed` → `Offline` in ~0.1 s, carrying the thread it had adopted, one state for the whole outage, and `Subscribed` again ~5 s after the wire returns. Nothing reads the file back, so it is a record rather than a pin; it carries no absolute path and no personal identifier. |


The pane captures come from `tmux capture-pane -p -J` against live `claude`
2.1.228 and 2.1.232 sessions. Personal identifiers are replaced by
length-preserving placeholders, so the pane geometry is byte-identical to what
was captured — column counts, wrap points and trailing padding all included,
which is the only reason a row-shaped check can be tested against them at all.

Four properties are worth stating explicitly, because all four were measured
rather than assumed and all four drive the design:

* `PermissionRequest` payloads carry **no `tool_use_id`**. The daemon recovers
  one by correlating with the `PreToolUse` that fires immediately before.
* `Notification` carries `notification_type`; `permission_prompt` is the push
  trigger and needs no inference from terminal output.
* A composer's footer hints are **not** a signal that it is there. `? for
  shortcuts` is dropped as soon as the shift+tab mode hint needs the room, and
  `← for agents` becomes `← 1 agent` once a subagent exists. The box is the
  invariant: at 3, 10 and 20 columns the composer keeps the same rule / `❯`
  prompt row / rule structure, so the check that reads it is width-invariant.
* A Codex `thread/started` is a **global broadcast**: every initialized connection
  receives it, subscribed or not, and it is the ONLY frame about a new thread that a
  connection subscribed elsewhere receives. That is what makes it usable as a switch
  signal and what makes ignoring it fatal.
* `thread/resume` **adds** a subscription rather than replacing one. One connection
  resumed thread A, followed a `/new` to B, resumed B on the same socket, and thereafter
  received both threads' streams. Following a switch therefore costs no reconnect — and
  the per-thread filter is what keeps the two apart.
* Both rows of that box start at **column zero**. An indented one is a
  quotation — agents print captured panes into their own output, and Claude
  indents tool results — so the anchor is what separates a composer that is
  there from a composer somebody wrote about.
