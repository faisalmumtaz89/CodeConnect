[package]
name = "protocol"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Shared CodeConnect types: event log envelope, unix-socket IPC frames, WSS messages."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = { workspace = true }

# build.rs computes the same ship-source fingerprint the library compares
# against at launch — one algorithm, two compile contexts.
[build-dependencies]
sha2 = { workspace = true }
